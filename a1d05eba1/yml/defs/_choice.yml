type: object
additionalProperties: false
required: ['value']
properties:
  $anchor:
    type: 'string'
  filters:
    type: 'object'
  label:
    $ref: '#/$defs/translatable'
  image:
    $ref: '#/$defs/translatable'
  audio:
    $ref: '#/$defs/translatable'
  value:
    type: string
    minLength: 1
  $autovalue:
    type: string
