# this is a reference to an attached file (as opposed to a remote file/URL)
anyOf:
- type: object
  required: ['file']
  additionalProperties: false
  properties:
    file:
      type: 'string'
      pattern: '^\w'
- type: string
  match: '/.*\.\w{3}/'
